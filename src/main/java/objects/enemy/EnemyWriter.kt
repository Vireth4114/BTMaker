package objects.enemy

import objects.GameObjectWriter
import java.io.DataOutput

class EnemyWriter: GameObjectWriter<Enemy> {
    override fun write(output: DataOutput, objectToWrite: Enemy) {

    }
}